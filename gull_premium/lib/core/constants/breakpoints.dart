/// Mobile breakpoint (≤768px). Use for 2-column grid, compact cards, tap targets.
const double kMobileBreakpoint = 768.0;

/// Tablet breakpoint (≤1024px). Use for 3-column grid. Above = desktop (4 columns).
const double kTabletBreakpoint = 1024.0;
